'use client'

// Placeholder — edición inline de hoyos (próxima iteración)
// Por ahora solo muestra un mensaje de ayuda para ADMIN
export default function CanchaClient({ courseId }: { courseId: number }) {
  void courseId
  return null
}
